{
  "name": "puhelinluettelo-backend",
  "version": "0.0.1",
  "description": "Backend for puhelinluettelo",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../fullstack/osa2/puhelinluettelo && npm run build --prod && cp -r build ../../../puhelinluettelo-backend/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "lint": "eslint ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mristeli/puhelinluettelo-backend.git"
  },
  "author": "Matti Risteli",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mristeli/puhelinluettelo-backend/issues"
  },
  "homepage": "https://github.com/mristeli/puhelinluettelo-backend#readme",
  "devDependencies": {
    "eslint": "^6.4.0",
    "nodemon": "^1.19.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.5",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.9.1"
  }
}
